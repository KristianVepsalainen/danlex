% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_references.R
\name{dlx_get_references}
\alias{dlx_get_references}
\title{Retrieve the references a document makes to other instruments}
\source{
Retsinformation, \url{https://www.retsinformation.dk}.
}
\usage{
dlx_get_references(
  collection = NULL,
  year = NULL,
  number = NULL,
  accn = NULL,
  eli = NULL
)
}
\arguments{
\item{collection}{Collection segment of the ELI URI. One of \code{"lta"}
(Lovtidende A: acts and regulations), \code{"ltb"}, \code{"ltc"} (treaties) or
\code{"mt"} (Ministerialtidende, discontinued in 2012). The collections
\code{"retsinfo"}, \code{"fob"} and \code{"ft"} are recognised but not yet supported.}

\item{year}{Four-digit year.}

\item{number}{Document number \emph{as published}. Character or numeric;
stored as character, because a small number of documents use
non-numeric forms such as \code{"1-1"}.}

\item{accn}{Accession number, e.g. \code{"A19990059229"}.}

\item{eli}{A full canonical ELI URI.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per reference:
\describe{
\item{\code{from_eli}}{ELI URI of the referring document.}
\item{\code{from_accn}}{Accession number of the referring document.}
\item{\code{ref_index}}{Position of the reference within the document.}
\item{\code{ref_accn}}{Accession number of the referenced instrument. Pass
this to \code{dlx_get_doc(accn = )} to follow the edge.}
\item{\code{ref_date}}{Date of the referenced version.}
\item{\code{ref_title}}{Title of the referenced instrument.}
}

Zero rows if the document has no references. A document that does not
exist yields zero rows and a warning.
}
\description{
Returns the cross-references recorded in a document's metadata as an edge
table, one row per reference. These are typically the enabling provisions
(\emph{hjemmel}) a regulation rests on.
}
\section{References are maintained, not historical}{


This is the single most important thing to understand about these edges.
Retsinformation keeps references pointing at the \emph{current} consolidated
version of the parent instrument, not at the version in force when the
referring document was issued.

A regulation from 1998, for instance, carries references to acts dated
1999 — the links were updated when the parent acts were re-consolidated.
Anyone building a citation network and treating the edges as timestamped to
the source document's date will get a systematically wrong picture of what
the law looked like at any past moment.

Use \code{ref_date} to see which version an edge currently points at. There is
no way to recover the reference as it stood at enactment from this API.
}

\section{Coverage}{


References are present on both sides of the 2007 text boundary — roughly
43\% of older and 59\% of modern documents carry at least one. A document
with no enabling provision simply has none, which is not an error.
}

\examples{
if (FALSE) {
  # A 1998 regulation resting on two acts
  refs <- dlx_get_references("lta", 1998, 763)

  # Follow the first edge
  dlx_get_doc(accn = refs$ref_accn[1])
}

}
\seealso{
\code{\link[=dlx_get_doc]{dlx_get_doc()}}, whose \code{n_references} column reports the row count
this function would return.
}
