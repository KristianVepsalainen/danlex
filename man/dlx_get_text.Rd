% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_text.R
\name{dlx_get_text}
\alias{dlx_get_text}
\title{Retrieve the full text of a document}
\source{
Retsinformation, \url{https://www.retsinformation.dk}.
}
\usage{
dlx_get_text(
  collection = NULL,
  year = NULL,
  number = NULL,
  accn = NULL,
  eli = NULL
)
}
\arguments{
\item{collection}{Collection segment of the ELI URI. One of \code{"lta"}
(Lovtidende A: acts and regulations), \code{"ltb"}, \code{"ltc"} (treaties) or
\code{"mt"} (Ministerialtidende, discontinued in 2012). The collections
\code{"retsinfo"}, \code{"fob"} and \code{"ft"} are recognised but not yet supported.}

\item{year}{Four-digit year.}

\item{number}{Document number \emph{as published}. Character or numeric;
stored as character, because a small number of documents use
non-numeric forms such as \code{"1-1"}.}

\item{accn}{Accession number, e.g. \code{"A19990059229"}.}

\item{eli}{A full canonical ELI URI.}
}
\value{
A one-row \link[tibble:tibble]{tibble} with \code{eli}, \code{status}
(\code{"ok"}, \code{"metadata_only"} or \code{"not_found"}), \code{n_char} and \code{text}.
}
\description{
Returns the document's operative text as a single string, rendered from the
structured XML.
}
\section{Text availability}{


Full content is available only for material published from late September
2007 onward; see \code{\link[=dlx_get_doc]{dlx_get_doc()}} for the boundary. Earlier documents return
\code{status = "metadata_only"} and \code{text = NA}.
}

\section{How the text is rendered}{


The source encodes text as \code{Linea} (lines) containing \code{Char} runs, with
\code{Explicatus} elements carrying structural markers such as \code{"§ 1"} or
\code{"Stk. 2."}. A naive \code{xml_text()} on the whole document runs these together
without separation, producing strings like \code{"§ 1I bekendtgørelse nr. ..."}.

danlex instead walks \code{Explicatus} and \code{Linea} in document order, joins them
with single spaces, and inserts a blank line before each marker that opens a
\code{Paragraf} or \code{Stk}. The result is readable running text with section
breaks preserved. Inline formatting (\code{formaChar}) is discarded.

For structured access to individual provisions, use \code{\link[=dlx_get_paragraphs]{dlx_get_paragraphs()}}.
}

\examples{
if (FALSE) {
  txt <- dlx_get_text("lta", 2025, 50)
  cat(substr(txt$text, 1, 500))

  # Pre-2008 documents carry metadata only
  dlx_get_text("lta", 1998, 763)$status
}

}
\seealso{
\code{\link[=dlx_get_paragraphs]{dlx_get_paragraphs()}} for provision-level output.
}
