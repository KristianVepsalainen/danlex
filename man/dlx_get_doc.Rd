% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_doc.R
\name{dlx_get_doc}
\alias{dlx_get_doc}
\title{Retrieve a Danish legal document}
\source{
Retsinformation, \url{https://www.retsinformation.dk}. Terms of use:
\url{https://www.retsinformation.dk/api}.
}
\usage{
dlx_get_doc(
  collection = NULL,
  year = NULL,
  number = NULL,
  accn = NULL,
  eli = NULL
)
}
\arguments{
\item{collection}{Collection segment of the ELI URI. One of \code{"lta"}
(Lovtidende A: acts and regulations), \code{"ltb"}, \code{"ltc"} (treaties) or
\code{"mt"} (Ministerialtidende, discontinued in 2012). The collections
\code{"retsinfo"}, \code{"fob"} and \code{"ft"} are recognised but not yet supported.}

\item{year}{Four-digit year.}

\item{number}{Document number \emph{as published}. Character or numeric;
stored as character, because a small number of documents use
non-numeric forms such as \code{"1-1"}.}

\item{accn}{Accession number, e.g. \code{"A19990059229"}.}

\item{eli}{A full canonical ELI URI.}
}
\value{
A one-row \link[tibble:tibble]{tibble} with, among others:
\describe{
\item{\code{status}}{\code{"ok"}, \code{"metadata_only"} or \code{"not_found"}.}
\item{\code{eli}}{Canonical ELI URI.}
\item{\code{collection}, \code{year}, \code{number}}{Identifier components.}
\item{\code{accn}}{Accession number.}
\item{\code{title}}{Document title.}
\item{\code{document_type}}{Type as recorded, verbatim.}
\item{\code{document_type_code}}{Internal code, post-2007 records only.}
\item{\code{document_type_std}}{Harmonised type label where known.}
\item{\code{legal_status}}{The source's \code{Status} field. Named
\code{legal_status} to avoid collision with \code{status} above.}
\item{\code{dies_signi}, \code{dies_edicti}}{Signing and publication dates.}
\item{\code{start_date}, \code{end_date}}{Period of validity.}
\item{\code{ministry}, \code{administrative_authority}}{Issuing body.}
\item{\code{has_text}}{Whether full content is available.}
\item{\code{n_references}}{Number of references to other instruments.}
}

A document that does not exist yields a one-row tibble with
\code{status = "not_found"} and all other fields missing, rather than an error.
}
\description{
Fetches a single document from Retsinformation and returns its metadata as
a one-row tibble. The document may be identified in any one of three ways:
by the deterministic triple of collection, year and number; by accession
number; or by a full ELI URI.
}
\section{Identifier forms}{


The deterministic form is the one users will normally have. A Danish act or
regulation is cited as, for example, "bekendtgørelse nr. 763 af 1998", which
maps directly onto \code{dlx_get_doc("lta", 1998, 763)}.

The accession form exists to follow references. \code{dlx_get_references()}
returns accession numbers rather than ELI URIs, so the citation network can
be traversed without a lookup table.

The ELI form is for URIs taken from \code{dlx_list_documents()} or
\code{dlx_get_changes()}.
}

\section{Text availability}{


Retsinformation's ELI service returns full document content only for
material published from roughly late September 2007 onward. The boundary
observed in Lovtidende A is act number 1081 of 2007, announced 25 September
2007. Earlier documents return metadata only, and \code{status} is then
\code{"metadata_only"} rather than \code{"ok"}.

This is a property of the source, not of danlex. Metadata for earlier
documents is complete and reaches back to 1852 for Lovtidende A.
}

\section{Instrument types}{


Lovtidende A carries both acts (\emph{love}) and regulations
(\emph{bekendtgørelser}) in a single number space, so the instrument type cannot
be inferred from the identifier. Always consult \code{document_type}.

Records also use two different schemas either side of the 2007 boundary:
older records give a whole Danish word, newer ones a composite code. Both
are returned verbatim in \code{document_type}, with the internal code split out
into \code{document_type_code} and a harmonised label in \code{document_type_std}.
}

\examples{
if (FALSE) {
  # A regulation from 1998 — metadata only, as it predates the text boundary
  dlx_get_doc("lta", 1998, 763)

  # A modern act — full content available
  dlx_get_doc("lta", 2025, 1)

  # Following a reference by accession number
  dlx_get_doc(accn = "A19990059229")

  # Straight from an ELI URI
  dlx_get_doc(eli = "https://retsinformation.dk/eli/lta/1998/763")
}

}
