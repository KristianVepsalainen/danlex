% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_docs.R
\name{dlx_get_docs}
\alias{dlx_get_docs}
\title{Retrieve many documents at once}
\usage{
dlx_get_docs(eli = NULL, accn = NULL, delay = 0.2, progress = interactive())
}
\arguments{
\item{eli}{Character vector of canonical ELI URIs.}

\item{accn}{Character vector of accession numbers. Supply this or \code{eli},
not both.}

\item{delay}{Seconds to pause between requests.}

\item{progress}{Show progress. Defaults to \code{\link[=interactive]{interactive()}}.}
}
\value{
A \link[tibble:tibble]{tibble} with the columns of \code{\link[=dlx_get_doc]{dlx_get_doc()}},
plus \code{error_message}, which is \code{NA} except on failed rows. One row per
input identifier, in input order. Duplicated identifiers are fetched
once and repeated in the output.
}
\description{
Fetches metadata for a vector of documents and returns one row per
identifier, in the order given. Intended for corpus-scale work: the output
of \code{\link[=dlx_list_documents]{dlx_list_documents()}} or the \code{ref_accn} column of
\code{\link[=dlx_get_references]{dlx_get_references()}} can be passed straight in.
}
\section{Failures do not stop the run}{


A batch of any size will eventually meet a network hiccup, and losing an
hour of retrieval to one dropped connection would be unacceptable. Failed
documents are therefore returned as rows with \code{status = "error"} and the
reason in \code{error_message}, rather than raised as conditions. Inspect them
with \code{subset(out, status == "error")} and retry those identifiers.

Missing documents behave as in \code{\link[=dlx_get_doc]{dlx_get_doc()}}: \code{status} is
\code{"not_found"}, which is a normal answer rather than a failure.
}

\section{Rate limiting}{


The ELI endpoints are not rate limited — the one-call-per-ten-seconds
restriction applies to the harvest service at \code{api.retsinformation.dk},
which danlex does not use. \code{delay} nevertheless defaults to a courtesy
pause of 0.2 seconds, since a batch of tens of thousands of requests
deserves some restraint. Retrieving 1,000 documents takes roughly five
minutes at the default.
}

\examples{
if (FALSE) {
  # Every act and regulation from one year
  idx  <- dlx_list_documents(year = 2025, collection = "lta")
  docs <- dlx_get_docs(idx$eli)

  # Anything that failed
  subset(docs, status == "error")

  # Follow a whole set of references
  refs <- dlx_get_references("lta", 1998, 763)
  dlx_get_docs(accn = refs$ref_accn)
}

}
\seealso{
\code{\link[=dlx_get_doc]{dlx_get_doc()}} for a single document, \code{\link[=dlx_list_documents]{dlx_list_documents()}}
for identifiers to feed in.
}
